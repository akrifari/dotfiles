# split tmux horizontally
#   @param $1 any number between 0 and 100
function tsh() {
  tmux split-window -h -p $1
}

# split tmux vertically
#  @param $1 any number between 0 and 100
function tsv() {
  tmux split-window -v -p $1
}
